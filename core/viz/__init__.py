"""Drawing helpers shared across components.

  - ``overlays``  -- ArUco quad + 3D axes + plane grid drawn through a
                     homography (used by every preview/pipeline app)
  - ``birdseye``  -- top-down marker-plane warp + inset embedding
"""
